# Assignment 03 — Neural Networks and Representation Learning
# Nguyễn Duy Nghĩa · B23DCCN600 · D23CTPM01

# --- Lõi tính toán: toàn bộ mạng nơ-ron được viết bằng NumPy thuần ---
numpy>=1.26
pandas>=2.1
matplotlib>=3.8

# --- Mô hình Machine Learning cổ điển dùng để đối chuẩn ---
# LƯU Ý: scikit-learn CHỈ dùng cho ba mô hình ML đối chứng (Logistic Regression,
# Decision Tree, Random Forest, Naive Bayes) và bộ vector hoá TF-IDF.
# KHÔNG dùng cho bất kỳ phần nào của mạng nơ-ron — mạng là 100% NumPy.
scikit-learn>=1.4
joblib>=1.3

# --- Notebook ---
jupyter>=1.0
nbformat>=5.9
nbconvert>=7.10
ipykernel>=6.28

# --- REST API + giao diện ---
Flask>=3.0
python-dotenv>=1.0

# --- Tầng đồ thị tri thức (tuỳ chọn — thiếu vẫn chạy được, chỉ mất phần tư vấn) ---
neo4j>=5.15

# --- Dựng báo cáo PDF ---
playwright>=1.40     # chụp ảnh màn hình giao diện Web/Mobile
pypdf>=4.0           # đánh số trang và dò vị trí mục lục
