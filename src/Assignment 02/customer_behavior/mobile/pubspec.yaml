name: customer_behavior_mobile
description: "Ứng dụng Flutter dự đoán khuyến nghị khách hàng — gọi REST API cùng backend với web UI."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
